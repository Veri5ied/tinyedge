{
  "name": "tinyedge",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "pnpm -r --filter ./apps/api... run build",
    "dev": "pnpm -r --filter ./apps/api... run dev"
  }
}
